#!/usr/bin/env bash
# Blocks `git push` if the test suite fails.
# Enable once with:  git config core.hooksPath hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: running tests..."
./run_tests.sh
